/**
 * ## Amit Raikwar
 *
 * Problem: Intersection and union.
 *
 * Approach (If repetition is allowed in the array):
 * - Brute force
 *   - [].[] sorting => Then finding the unique element for union.
 *
 * - Better approach
 *  - Finding unique element for the both the element
 *  - Using two pointer approach
 *    - Check move the pointer until we see same element.
 *
 * - Optimised approach
 *  - Using two pointer
 *    - Push the element if
 */